import type MockAdapter from 'axios-mock-adapter';

/**
 * Register device management mock routes.
 */
export function setupDeviceMock(mock: MockAdapter): void {
  // TODO: implement device mock routes
  void mock;
}
